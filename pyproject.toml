[tool.poetry]
name = "role-sentry"
version = "0.1.0"
description = "Web UI Dashboard for monitoring role sizes and refactor status"

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.dev-dependencies]
pytest = "^7.1"
